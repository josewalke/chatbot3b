# Requirements optimizados para Chatbot Inteligente
# Solo dependencias esenciales para ahorrar recursos

# Framework web principal
fastapi==0.88.0
uvicorn[standard]==0.20.0

# Utilidades básicas
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.32.4

# Base de datos ligera (sqlite3 viene incluido en Python)
# No necesita instalación

# Logging y manejo de errores
# (incluido en Python)

# Procesamiento de texto básico
# (incluido en Python)

# Configuración opcional (descomentar solo si se necesita)
# openai==1.3.7  # Solo si se usa OpenAI
# stripe==7.8.0  # Solo si se usan pagos
# python-telegram-bot==20.7  # Solo si se usa Telegram
# facebook-sdk==3.1.0  # Solo si se usa Facebook

# Dependencias de desarrollo (opcional)
# pytest==7.4.0  # Solo para testing
# black==23.3.0  # Solo para formateo de código 